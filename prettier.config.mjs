/** @type {import('prettier').Config} */
const config = {
  arrowParens: 'avoid',
  bracketSpacing: true,
  printWidth: 90,
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: 'all'
};

export default config;
